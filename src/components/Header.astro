---
import { nav } from '../lib/config';

const currentPath = Astro.url.pathname;
function isActive(href: string) {
  if (href === '/') return currentPath === '/';
  return currentPath.startsWith(href);
}
---

<div id="mobile-menu-root" class="contents">
<header class="sticky top-0 z-40 border-b border-glass-border bg-paper/70 shadow-glass backdrop-blur-xl">
  <div class="mx-auto max-w-5xl px-6 py-4">
    <input type="checkbox" id="nav-toggle" class="peer hidden" />

    <div class="flex items-center justify-between">
      <a href="/" class="flex items-center gap-2 font-display text-xl font-semibold tracking-tight text-ink">
        <img src="/favicon.svg" alt="" width="24" height="24" class="h-6 w-6 rounded" />
        chisamikan.site
      </a>

      <div class="flex items-center gap-1 sm:gap-3">
        <nav aria-label="サイト内ナビゲーション" class="hidden sm:block">
          <ul class="flex items-center gap-1 font-mono text-xs uppercase tracking-wider sm:gap-2">
            {nav.map((item) => (
              <li>
                <a
                  href={item.href}
                  aria-current={isActive(item.href) ? 'page' : undefined}
                  class:list={[
                    'relative rounded px-2.5 py-1.5 transition-colors hover:text-accent sm:px-3',
                    isActive(item.href) ? 'text-accent' : 'text-ink/70',
                  ]}
                >
                  {item.label}
                  {isActive(item.href) && (
                    <span class="absolute inset-x-2 -bottom-0.5 h-0.5 bg-accent" aria-hidden="true"></span>
                  )}
                </a>
              </li>
            ))}
          </ul>
        </nav>

        <label
          for="nav-toggle"
          class="nav-icon-toggle flex h-9 w-9 shrink-0 cursor-pointer items-center justify-center rounded text-ink/70 transition-colors hover:text-accent sm:hidden"
          aria-label="メニューを開閉する"
        >
          <span class="nav-icon-bars">
            <span></span>
            <span></span>
            <span></span>
            <span></span>
          </span>
        </label>

        <button
          type="button"
          aria-label="Dark Mode"
          aria-pressed="false"
          class="theme-toggle-btn hidden h-9 w-9 shrink-0 items-center justify-center rounded text-ink/70 transition-colors hover:text-accent sm:flex"
        >
          <!--
            FontAwesomeのCSSはTailwindのcascade layerの外側で読み込まれるため、
            レイヤーに属さないスタイルは常にレイヤー内のユーティリティより優先されてしまう
            (詳細: https://developer.mozilla.org/docs/Web/CSS/@layer)。
            .fa-regular が display: inline-block を無条件に指定してくるため、
            dark: の display 切り替えが効かず両方のアイコンが表示され続ける問題があった。
            important修飾子(末尾の!)で明示的に優先させる。
          -->
          <i class="fa-regular fa-moon dark:hidden!" aria-hidden="true"></i>
          <i class="fa-regular fa-sun hidden! dark:inline!" aria-hidden="true"></i>
        </button>
      </div>
    </div>
  </div>
</header>

<nav
  aria-label="サイト内ナビゲーション（モバイル）"
  class="mobile-nav-overlay pointer-events-none fixed inset-0 z-30 flex -translate-y-4 flex-col items-center justify-center gap-1 overflow-y-auto bg-paper/70 px-6 py-24 opacity-0 shadow-glass backdrop-blur-xl transition duration-300 ease-out sm:hidden"
>
  <ul class="flex flex-col items-center gap-2 font-mono text-xs uppercase tracking-wider">
    {nav.map((item) => (
      <li>
        <a
          href={item.href}
          aria-current={isActive(item.href) ? 'page' : undefined}
          class:list={[
            'relative inline-block rounded px-3 py-2 transition-colors hover:text-accent',
            isActive(item.href) ? 'text-accent' : 'text-ink/70',
          ]}
        >
          {item.label}
        </a>
      </li>
    ))}
    <li>
      <button
        type="button"
        aria-label="Dark Mode"
        aria-pressed="false"
        class="theme-toggle-btn inline-flex items-center gap-2 rounded px-3 py-2 uppercase text-ink/70 transition-colors hover:text-accent"
      >
        <i class="fa-regular fa-moon dark:hidden!" aria-hidden="true"></i>
        <i class="fa-regular fa-sun hidden! dark:inline!" aria-hidden="true"></i>
        <span class="dark:hidden">Dark Mode</span>
        <span class="hidden dark:inline">Light Mode</span>
      </button>
    </li>
  </ul>
</nav>
</div>

<style>
  :global(body:has(#nav-toggle:checked)) {
    overflow: hidden;
  }

  /* #nav-toggle and .mobile-nav-overlay are siblings of <header>, not of
     each other (the overlay must sit outside header's backdrop-filter,
     which otherwise clips a fixed-position descendant's containing block
     to header's own small box instead of the viewport). peer-checked
     can't reach across that gap, so :has() on the shared wrapper does it
     instead. */
  #mobile-menu-root:has(#nav-toggle:checked) .mobile-nav-overlay {
    pointer-events: auto;
    opacity: 1;
    transform: translateY(0);
  }

  .nav-icon-bars {
    position: relative;
    display: block;
    width: 20px;
    height: 16px;
  }

  .nav-icon-bars span {
    position: absolute;
    left: 0;
    width: 100%;
    height: 2px;
    border-radius: 2px;
    background: currentColor;
    transition: 0.25s ease-in-out;
  }

  .nav-icon-bars span:nth-child(1) {
    top: 0;
  }

  .nav-icon-bars span:nth-child(2),
  .nav-icon-bars span:nth-child(3) {
    top: 7px;
  }

  /* span:nth-child(3) exactly overlaps span:nth-child(2); with the
     semi-transparent ink color, showing both at once stacks the
     alpha and makes the middle bar look darker than the others. */
  .nav-icon-bars span:nth-child(3) {
    opacity: 0;
  }

  .nav-icon-bars span:nth-child(4) {
    top: 14px;
  }

  #nav-toggle:checked ~ div .nav-icon-bars span:nth-child(1) {
    top: 7px;
    width: 0;
    left: 50%;
  }

  #nav-toggle:checked ~ div .nav-icon-bars span:nth-child(2) {
    transform: rotate(45deg);
  }

  #nav-toggle:checked ~ div .nav-icon-bars span:nth-child(3) {
    opacity: 1;
    transform: rotate(-45deg);
  }

  #nav-toggle:checked ~ div .nav-icon-bars span:nth-child(4) {
    top: 7px;
    width: 0;
    left: 50%;
  }
</style>

<script>
  const toggles = document.querySelectorAll<HTMLButtonElement>('.theme-toggle-btn');
  const media = window.matchMedia('(prefers-color-scheme: dark)');

  function applyState(isDark: boolean) {
    document.documentElement.classList.toggle('dark', isDark);
    toggles.forEach((toggle) => {
      toggle.setAttribute('aria-pressed', String(isDark));
      toggle.setAttribute('aria-label', isDark ? 'Light Mode' : 'Dark Mode');
    });
  }

  applyState(document.documentElement.classList.contains('dark'));

  toggles.forEach((toggle) => {
    toggle.addEventListener('click', () => {
      const isDark = !document.documentElement.classList.contains('dark');
      localStorage.setItem('theme', isDark ? 'dark' : 'light');
      applyState(isDark);
    });
  });

  // 明示的な選択(localStorageの保存値)が無い場合のみ、端末側の設定変更に追従する
  media.addEventListener('change', (event) => {
    if (localStorage.getItem('theme')) return;
    applyState(event.matches);
  });

  // ページ内アンカー(例: /#news)はページ遷移が起きず全画面メニューが開いたままになるため、
  // タップ時に明示的にメニューを閉じる。別ページへのリンクはページ遷移がそのまま状態をリセットするので対象外。
  const navToggle = document.getElementById('nav-toggle') as HTMLInputElement | null;
  const mobileNavLinks = document.querySelectorAll<HTMLAnchorElement>('.mobile-nav-overlay a');
  const currentPath = window.location.pathname;

  mobileNavLinks.forEach((link) => {
    const isSamePageAnchor = link.pathname === currentPath && link.hash !== '';
    if (!isSamePageAnchor) return;

    link.addEventListener('click', () => {
      if (navToggle) navToggle.checked = false;
    });
  });
</script>
